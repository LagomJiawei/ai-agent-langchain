# ==============================================================================
# LiCaiManus - 环境配置文件
# LangChain + LangGraph 版本
# ==============================================================================

# ------------------------------------------------------------------------------
# LLM 模型配置（通义千问，兼容 OpenAI 接口）
# ------------------------------------------------------------------------------
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL_NAME=qwen-turbo
TEMPERATURE=0.7
TIMEOUT=60

# Embedding 模型配置
EMBEDDING_MODEL_NAME=text-embedding-v3

# ------------------------------------------------------------------------------
# SearchAPI 配置（用于网页搜索工具）
# ------------------------------------------------------------------------------
SEARCH_API_KEY=your_searchapi_key_here

# ------------------------------------------------------------------------------
# 向量数据库配置
# ------------------------------------------------------------------------------
VECTOR_STORE_TYPE=inmemory  # 可选: inmemory, milvus

# Milvus 配置（VECTOR_STORE_TYPE=milvus 时使用）
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_COLLECTION_NAME=rag_documents
MILVUS_DIMENSION=1024

# ------------------------------------------------------------------------------
# Redis 配置（用于 Embedding 缓存、对话记忆、语义结果缓存）
# ------------------------------------------------------------------------------
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ------------------------------------------------------------------------------
# 聊天记忆配置
# ------------------------------------------------------------------------------
CHAT_MEMORY_STORE_TYPE=file  # 可选: memory, file, redis
CHAT_MEMORY_MAX_MESSAGES=20
CHAT_MEMORY_BASE_DIR=./chat-memory

# ------------------------------------------------------------------------------
# RAG 检索增强配置
# ------------------------------------------------------------------------------
RAG_TOP_K=5  # 最终上下文文档数量
RAG_RECALL_K=50  # 候选召回数量
RAG_QUALITY_THRESHOLD=0.35  # Agentic RAG 信息充足性阈值

# ------------------------------------------------------------------------------
# Agent 配置
# ------------------------------------------------------------------------------
AGENT_MAX_ITERATIONS=10  # 最大迭代次数
AGENT_MODE=auto  # 可选: auto, react, plan_execute

# ------------------------------------------------------------------------------
# 工具限流配置
# ------------------------------------------------------------------------------
TOOL_RATE_LIMIT_ENABLED=true
TOOL_RATE_LIMIT_QPS=5  # 每秒最多调用次数
TOOL_RATE_LIMIT_MAX_CONCURRENT=10  # 最大并发数
TOOL_RATE_LIMIT_CIRCUIT_BREAKER_THRESHOLD=5  # 熔断失败阈值

# ------------------------------------------------------------------------------
# 应用服务配置
# ------------------------------------------------------------------------------
APP_HOST=0.0.0.0
APP_PORT=8000
APP_DEBUG=true
